version: "3.9"

services:
  backend:
    build: ./backend
    container_name: student-backend
    ports:
      - "5005:5000"
    restart: always

  frontend:
    build: ./frontend
    container_name: student-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
